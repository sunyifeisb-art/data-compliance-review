[phases.setup]
nixPkgs = ["python311", "uv"]

[phases.install]
cmds = ["cd data-compliance-web && uv pip install -r requirements.txt --system"]

[start]
cmd = "cd data-compliance-web && gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
