[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["cd data-compliance-web && PIP_REQUIRE_VIRTUALENV=0 pip install -r requirements.txt"]

[start]
cmd = "cd data-compliance-web && gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
