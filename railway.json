{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd data-compliance-web && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd data-compliance-web && gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120",
    "healthcheckPath": "/"
  }
}
